using UnityEngine;

public static class Globals
{
    public static int Region { get; set; } = 0; // 0 means first region (only normal dragoon), 1 means second region (long dragoon and normal), etc
}
